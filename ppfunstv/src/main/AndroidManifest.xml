<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ppfuns.ppfunstv">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <service
            android:name=".service.MarqueeService"
            android:process="com.ppfuns.launcher.marqueeservice">
            <intent-filter>
                <action android:name="com.ppfuns.action.MarqueeService" />
            </intent-filter>
        </service>
    </application>

</manifest>
